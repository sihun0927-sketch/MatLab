% 4.1 연습문제 3 - 방향이 맞지 않는 콜론 연산자는 빈 배열이 된다
b = 1:-1:5                  % 1에서 시작해 -1씩 줄어들며 5에 도달할 수 없다
sizeB = size(b)              % 1x0
isEmptyB = isempty(b)        % true
% 에러가 나지 않는 이유: MATLAB의 콜론 연산자는 조건을 만족하는 값이
% 하나도 없으면 그냥 "빈 배열"을 반환하도록 정의되어 있기 때문이다.

% Ch4.3 - zeros, ones로 특수 배열 만들기

Z1 = zeros(3)                 % 3x3, 전부 0
Z2 = zeros(2, 4)              % 2x4, 전부 0

O1 = ones(3)                  % 3x3, 전부 1
O2 = ones(2, 4)               % 2x4, 전부 1
